<?php

/**
 * Base class that represents a row from the 'tabulado' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * Sun Jun  6 23:20:57 2010
 *
 * @package    lib.model.profit.om
 */
abstract class BaseTabulado extends BaseObject  implements Persistent {


  const PEER = 'TabuladoPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        TabuladoPeer
	 */
	protected static $peer;

	/**
	 * The value for the tipo field.
	 * @var        string
	 */
	protected $tipo;

	/**
	 * The value for the descripcio field.
	 * Note: this column has a database default value of: ' '
	 * @var        string
	 */
	protected $descripcio;

	/**
	 * The value for the porc_vent field.
	 * Note: this column has a database default value of: '0'
	 * @var        string
	 */
	protected $porc_vent;

	/**
	 * The value for the porc_comp field.
	 * Note: this column has a database default value of: '0'
	 * @var        string
	 */
	protected $porc_comp;

	/**
	 * The value for the porc_cxs field.
	 * Note: this column has a database default value of: '0'
	 * @var        string
	 */
	protected $porc_cxs;

	/**
	 * The value for the porc_otro field.
	 * Note: this column has a database default value of: '0'
	 * @var        string
	 */
	protected $porc_otro;

	/**
	 * The value for the revisado field.
	 * Note: this column has a database default value of: ' '
	 * @var        string
	 */
	protected $revisado;

	/**
	 * The value for the trasnfe field.
	 * Note: this column has a database default value of: ' '
	 * @var        string
	 */
	protected $trasnfe;

	/**
	 * The value for the rowguid field.
	 * Note: this column has a database default value of: '(newid())'
	 * @var        string
	 */
	protected $rowguid;

	/**
	 * @var        array Art[] Collection to store aggregation of Art objects.
	 */
	protected $collArts;

	/**
	 * @var        Criteria The criteria used to select the current contents of collArts.
	 */
	private $lastArtCriteria = null;

	/**
	 * @var        array RengFac[] Collection to store aggregation of RengFac objects.
	 */
	protected $collRengFacs;

	/**
	 * @var        Criteria The criteria used to select the current contents of collRengFacs.
	 */
	private $lastRengFacCriteria = null;

	/**
	 * @var        array DocumCc[] Collection to store aggregation of DocumCc objects.
	 */
	protected $collDocumCcs;

	/**
	 * @var        Criteria The criteria used to select the current contents of collDocumCcs.
	 */
	private $lastDocumCcCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BaseTabulado object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->descripcio = ' ';
		$this->porc_vent = '0';
		$this->porc_comp = '0';
		$this->porc_cxs = '0';
		$this->porc_otro = '0';
		$this->revisado = ' ';
		$this->trasnfe = ' ';
		$this->rowguid = '(newid())';
	}

	/**
	 * Get the [tipo] column value.
	 * 
	 * @return     string
	 */
	public function getTipo()
	{
		return $this->tipo;
	}

	/**
	 * Get the [descripcio] column value.
	 * 
	 * @return     string
	 */
	public function getDescripcio()
	{
		return $this->descripcio;
	}

	/**
	 * Get the [porc_vent] column value.
	 * 
	 * @return     string
	 */
	public function getPorcVent()
	{
		return $this->porc_vent;
	}

	/**
	 * Get the [porc_comp] column value.
	 * 
	 * @return     string
	 */
	public function getPorcComp()
	{
		return $this->porc_comp;
	}

	/**
	 * Get the [porc_cxs] column value.
	 * 
	 * @return     string
	 */
	public function getPorcCxs()
	{
		return $this->porc_cxs;
	}

	/**
	 * Get the [porc_otro] column value.
	 * 
	 * @return     string
	 */
	public function getPorcOtro()
	{
		return $this->porc_otro;
	}

	/**
	 * Get the [revisado] column value.
	 * 
	 * @return     string
	 */
	public function getRevisado()
	{
		return $this->revisado;
	}

	/**
	 * Get the [trasnfe] column value.
	 * 
	 * @return     string
	 */
	public function getTrasnfe()
	{
		return $this->trasnfe;
	}

	/**
	 * Get the [rowguid] column value.
	 * 
	 * @return     string
	 */
	public function getRowguid()
	{
		return $this->rowguid;
	}

	/**
	 * Set the value of [tipo] column.
	 * 
	 * @param      string $v new value
	 * @return     Tabulado The current object (for fluent API support)
	 */
	public function setTipo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->tipo !== $v) {
			$this->tipo = $v;
			$this->modifiedColumns[] = TabuladoPeer::TIPO;
		}

		return $this;
	} // setTipo()

	/**
	 * Set the value of [descripcio] column.
	 * 
	 * @param      string $v new value
	 * @return     Tabulado The current object (for fluent API support)
	 */
	public function setDescripcio($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->descripcio !== $v || $v === ' ') {
			$this->descripcio = $v;
			$this->modifiedColumns[] = TabuladoPeer::DESCRIPCIO;
		}

		return $this;
	} // setDescripcio()

	/**
	 * Set the value of [porc_vent] column.
	 * 
	 * @param      string $v new value
	 * @return     Tabulado The current object (for fluent API support)
	 */
	public function setPorcVent($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->porc_vent !== $v || $v === '0') {
			$this->porc_vent = $v;
			$this->modifiedColumns[] = TabuladoPeer::PORC_VENT;
		}

		return $this;
	} // setPorcVent()

	/**
	 * Set the value of [porc_comp] column.
	 * 
	 * @param      string $v new value
	 * @return     Tabulado The current object (for fluent API support)
	 */
	public function setPorcComp($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->porc_comp !== $v || $v === '0') {
			$this->porc_comp = $v;
			$this->modifiedColumns[] = TabuladoPeer::PORC_COMP;
		}

		return $this;
	} // setPorcComp()

	/**
	 * Set the value of [porc_cxs] column.
	 * 
	 * @param      string $v new value
	 * @return     Tabulado The current object (for fluent API support)
	 */
	public function setPorcCxs($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->porc_cxs !== $v || $v === '0') {
			$this->porc_cxs = $v;
			$this->modifiedColumns[] = TabuladoPeer::PORC_CXS;
		}

		return $this;
	} // setPorcCxs()

	/**
	 * Set the value of [porc_otro] column.
	 * 
	 * @param      string $v new value
	 * @return     Tabulado The current object (for fluent API support)
	 */
	public function setPorcOtro($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->porc_otro !== $v || $v === '0') {
			$this->porc_otro = $v;
			$this->modifiedColumns[] = TabuladoPeer::PORC_OTRO;
		}

		return $this;
	} // setPorcOtro()

	/**
	 * Set the value of [revisado] column.
	 * 
	 * @param      string $v new value
	 * @return     Tabulado The current object (for fluent API support)
	 */
	public function setRevisado($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->revisado !== $v || $v === ' ') {
			$this->revisado = $v;
			$this->modifiedColumns[] = TabuladoPeer::REVISADO;
		}

		return $this;
	} // setRevisado()

	/**
	 * Set the value of [trasnfe] column.
	 * 
	 * @param      string $v new value
	 * @return     Tabulado The current object (for fluent API support)
	 */
	public function setTrasnfe($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->trasnfe !== $v || $v === ' ') {
			$this->trasnfe = $v;
			$this->modifiedColumns[] = TabuladoPeer::TRASNFE;
		}

		return $this;
	} // setTrasnfe()

	/**
	 * Set the value of [rowguid] column.
	 * 
	 * @param      string $v new value
	 * @return     Tabulado The current object (for fluent API support)
	 */
	public function setRowguid($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->rowguid !== $v || $v === '(newid())') {
			$this->rowguid = $v;
			$this->modifiedColumns[] = TabuladoPeer::ROWGUID;
		}

		return $this;
	} // setRowguid()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array(TabuladoPeer::DESCRIPCIO,TabuladoPeer::PORC_VENT,TabuladoPeer::PORC_COMP,TabuladoPeer::PORC_CXS,TabuladoPeer::PORC_OTRO,TabuladoPeer::REVISADO,TabuladoPeer::TRASNFE,TabuladoPeer::ROWGUID))) {
				return false;
			}

			if ($this->descripcio !== ' ') {
				return false;
			}

			if ($this->porc_vent !== '0') {
				return false;
			}

			if ($this->porc_comp !== '0') {
				return false;
			}

			if ($this->porc_cxs !== '0') {
				return false;
			}

			if ($this->porc_otro !== '0') {
				return false;
			}

			if ($this->revisado !== ' ') {
				return false;
			}

			if ($this->trasnfe !== ' ') {
				return false;
			}

			if ($this->rowguid !== '(newid())') {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->tipo = ($row[$startcol + 0] !== null) ? (string) $row[$startcol + 0] : null;
			$this->descripcio = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->porc_vent = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->porc_comp = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->porc_cxs = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->porc_otro = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->revisado = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->trasnfe = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->rowguid = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 9; // 9 = TabuladoPeer::NUM_COLUMNS - TabuladoPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Tabulado object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(TabuladoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = TabuladoPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->collArts = null;
			$this->lastArtCriteria = null;

			$this->collRengFacs = null;
			$this->lastRengFacCriteria = null;

			$this->collDocumCcs = null;
			$this->lastDocumCcCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseTabulado:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(TabuladoPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			TabuladoPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseTabulado:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseTabulado:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(TabuladoPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseTabulado:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			TabuladoPeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;


			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = TabuladoPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setNew(false);
				} else {
					$affectedRows += TabuladoPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collArts !== null) {
				foreach ($this->collArts as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collRengFacs !== null) {
				foreach ($this->collRengFacs as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collDocumCcs !== null) {
				foreach ($this->collDocumCcs as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			if (($retval = TabuladoPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collArts !== null) {
					foreach ($this->collArts as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collRengFacs !== null) {
					foreach ($this->collRengFacs as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collDocumCcs !== null) {
					foreach ($this->collDocumCcs as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = TabuladoPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getTipo();
				break;
			case 1:
				return $this->getDescripcio();
				break;
			case 2:
				return $this->getPorcVent();
				break;
			case 3:
				return $this->getPorcComp();
				break;
			case 4:
				return $this->getPorcCxs();
				break;
			case 5:
				return $this->getPorcOtro();
				break;
			case 6:
				return $this->getRevisado();
				break;
			case 7:
				return $this->getTrasnfe();
				break;
			case 8:
				return $this->getRowguid();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = TabuladoPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getTipo(),
			$keys[1] => $this->getDescripcio(),
			$keys[2] => $this->getPorcVent(),
			$keys[3] => $this->getPorcComp(),
			$keys[4] => $this->getPorcCxs(),
			$keys[5] => $this->getPorcOtro(),
			$keys[6] => $this->getRevisado(),
			$keys[7] => $this->getTrasnfe(),
			$keys[8] => $this->getRowguid(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = TabuladoPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setTipo($value);
				break;
			case 1:
				$this->setDescripcio($value);
				break;
			case 2:
				$this->setPorcVent($value);
				break;
			case 3:
				$this->setPorcComp($value);
				break;
			case 4:
				$this->setPorcCxs($value);
				break;
			case 5:
				$this->setPorcOtro($value);
				break;
			case 6:
				$this->setRevisado($value);
				break;
			case 7:
				$this->setTrasnfe($value);
				break;
			case 8:
				$this->setRowguid($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = TabuladoPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setTipo($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setDescripcio($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setPorcVent($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setPorcComp($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setPorcCxs($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setPorcOtro($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setRevisado($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setTrasnfe($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setRowguid($arr[$keys[8]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);

		if ($this->isColumnModified(TabuladoPeer::TIPO)) $criteria->add(TabuladoPeer::TIPO, $this->tipo);
		if ($this->isColumnModified(TabuladoPeer::DESCRIPCIO)) $criteria->add(TabuladoPeer::DESCRIPCIO, $this->descripcio);
		if ($this->isColumnModified(TabuladoPeer::PORC_VENT)) $criteria->add(TabuladoPeer::PORC_VENT, $this->porc_vent);
		if ($this->isColumnModified(TabuladoPeer::PORC_COMP)) $criteria->add(TabuladoPeer::PORC_COMP, $this->porc_comp);
		if ($this->isColumnModified(TabuladoPeer::PORC_CXS)) $criteria->add(TabuladoPeer::PORC_CXS, $this->porc_cxs);
		if ($this->isColumnModified(TabuladoPeer::PORC_OTRO)) $criteria->add(TabuladoPeer::PORC_OTRO, $this->porc_otro);
		if ($this->isColumnModified(TabuladoPeer::REVISADO)) $criteria->add(TabuladoPeer::REVISADO, $this->revisado);
		if ($this->isColumnModified(TabuladoPeer::TRASNFE)) $criteria->add(TabuladoPeer::TRASNFE, $this->trasnfe);
		if ($this->isColumnModified(TabuladoPeer::ROWGUID)) $criteria->add(TabuladoPeer::ROWGUID, $this->rowguid);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);

		$criteria->add(TabuladoPeer::TIPO, $this->tipo);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     string
	 */
	public function getPrimaryKey()
	{
		return $this->getTipo();
	}

	/**
	 * Generic method to set the primary key (tipo column).
	 *
	 * @param      string $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setTipo($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Tabulado (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setTipo($this->tipo);

		$copyObj->setDescripcio($this->descripcio);

		$copyObj->setPorcVent($this->porc_vent);

		$copyObj->setPorcComp($this->porc_comp);

		$copyObj->setPorcCxs($this->porc_cxs);

		$copyObj->setPorcOtro($this->porc_otro);

		$copyObj->setRevisado($this->revisado);

		$copyObj->setTrasnfe($this->trasnfe);

		$copyObj->setRowguid($this->rowguid);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getArts() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addArt($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getRengFacs() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addRengFac($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getDocumCcs() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addDocumCc($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Tabulado Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     TabuladoPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new TabuladoPeer();
		}
		return self::$peer;
	}

	/**
	 * Clears out the collArts collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addArts()
	 */
	public function clearArts()
	{
		$this->collArts = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collArts collection (array).
	 *
	 * By default this just sets the collArts collection to an empty array (like clearcollArts());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initArts()
	{
		$this->collArts = array();
	}

	/**
	 * Gets an array of Art objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Tabulado has previously been saved, it will retrieve
	 * related Arts from storage. If this Tabulado is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Art[]
	 * @throws     PropelException
	 */
	public function getArts($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collArts === null) {
			if ($this->isNew()) {
			   $this->collArts = array();
			} else {

				$criteria->add(ArtPeer::TIPO_IMP, $this->tipo);

				ArtPeer::addSelectColumns($criteria);
				$this->collArts = ArtPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ArtPeer::TIPO_IMP, $this->tipo);

				ArtPeer::addSelectColumns($criteria);
				if (!isset($this->lastArtCriteria) || !$this->lastArtCriteria->equals($criteria)) {
					$this->collArts = ArtPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastArtCriteria = $criteria;
		return $this->collArts;
	}

	/**
	 * Returns the number of related Art objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Art objects.
	 * @throws     PropelException
	 */
	public function countArts(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collArts === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ArtPeer::TIPO_IMP, $this->tipo);

				$count = ArtPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ArtPeer::TIPO_IMP, $this->tipo);

				if (!isset($this->lastArtCriteria) || !$this->lastArtCriteria->equals($criteria)) {
					$count = ArtPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collArts);
				}
			} else {
				$count = count($this->collArts);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Art object to this object
	 * through the Art foreign key attribute.
	 *
	 * @param      Art $l Art
	 * @return     void
	 * @throws     PropelException
	 */
	public function addArt(Art $l)
	{
		if ($this->collArts === null) {
			$this->initArts();
		}
		if (!in_array($l, $this->collArts, true)) { // only add it if the **same** object is not already associated
			array_push($this->collArts, $l);
			$l->setTabulado($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tabulado is new, it will return
	 * an empty collection; or if this Tabulado has previously
	 * been saved, it will retrieve related Arts from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tabulado.
	 */
	public function getArtsJoinLinArt($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collArts === null) {
			if ($this->isNew()) {
				$this->collArts = array();
			} else {

				$criteria->add(ArtPeer::TIPO_IMP, $this->tipo);

				$this->collArts = ArtPeer::doSelectJoinLinArt($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ArtPeer::TIPO_IMP, $this->tipo);

			if (!isset($this->lastArtCriteria) || !$this->lastArtCriteria->equals($criteria)) {
				$this->collArts = ArtPeer::doSelectJoinLinArt($criteria, $con, $join_behavior);
			}
		}
		$this->lastArtCriteria = $criteria;

		return $this->collArts;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tabulado is new, it will return
	 * an empty collection; or if this Tabulado has previously
	 * been saved, it will retrieve related Arts from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tabulado.
	 */
	public function getArtsJoinCatArt($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collArts === null) {
			if ($this->isNew()) {
				$this->collArts = array();
			} else {

				$criteria->add(ArtPeer::TIPO_IMP, $this->tipo);

				$this->collArts = ArtPeer::doSelectJoinCatArt($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ArtPeer::TIPO_IMP, $this->tipo);

			if (!isset($this->lastArtCriteria) || !$this->lastArtCriteria->equals($criteria)) {
				$this->collArts = ArtPeer::doSelectJoinCatArt($criteria, $con, $join_behavior);
			}
		}
		$this->lastArtCriteria = $criteria;

		return $this->collArts;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tabulado is new, it will return
	 * an empty collection; or if this Tabulado has previously
	 * been saved, it will retrieve related Arts from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tabulado.
	 */
	public function getArtsJoinSubLin($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collArts === null) {
			if ($this->isNew()) {
				$this->collArts = array();
			} else {

				$criteria->add(ArtPeer::TIPO_IMP, $this->tipo);

				$this->collArts = ArtPeer::doSelectJoinSubLin($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ArtPeer::TIPO_IMP, $this->tipo);

			if (!isset($this->lastArtCriteria) || !$this->lastArtCriteria->equals($criteria)) {
				$this->collArts = ArtPeer::doSelectJoinSubLin($criteria, $con, $join_behavior);
			}
		}
		$this->lastArtCriteria = $criteria;

		return $this->collArts;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tabulado is new, it will return
	 * an empty collection; or if this Tabulado has previously
	 * been saved, it will retrieve related Arts from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tabulado.
	 */
	public function getArtsJoinColores($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collArts === null) {
			if ($this->isNew()) {
				$this->collArts = array();
			} else {

				$criteria->add(ArtPeer::TIPO_IMP, $this->tipo);

				$this->collArts = ArtPeer::doSelectJoinColores($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ArtPeer::TIPO_IMP, $this->tipo);

			if (!isset($this->lastArtCriteria) || !$this->lastArtCriteria->equals($criteria)) {
				$this->collArts = ArtPeer::doSelectJoinColores($criteria, $con, $join_behavior);
			}
		}
		$this->lastArtCriteria = $criteria;

		return $this->collArts;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tabulado is new, it will return
	 * an empty collection; or if this Tabulado has previously
	 * been saved, it will retrieve related Arts from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tabulado.
	 */
	public function getArtsJoinProceden($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collArts === null) {
			if ($this->isNew()) {
				$this->collArts = array();
			} else {

				$criteria->add(ArtPeer::TIPO_IMP, $this->tipo);

				$this->collArts = ArtPeer::doSelectJoinProceden($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ArtPeer::TIPO_IMP, $this->tipo);

			if (!isset($this->lastArtCriteria) || !$this->lastArtCriteria->equals($criteria)) {
				$this->collArts = ArtPeer::doSelectJoinProceden($criteria, $con, $join_behavior);
			}
		}
		$this->lastArtCriteria = $criteria;

		return $this->collArts;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tabulado is new, it will return
	 * an empty collection; or if this Tabulado has previously
	 * been saved, it will retrieve related Arts from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tabulado.
	 */
	public function getArtsJoinProv($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collArts === null) {
			if ($this->isNew()) {
				$this->collArts = array();
			} else {

				$criteria->add(ArtPeer::TIPO_IMP, $this->tipo);

				$this->collArts = ArtPeer::doSelectJoinProv($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ArtPeer::TIPO_IMP, $this->tipo);

			if (!isset($this->lastArtCriteria) || !$this->lastArtCriteria->equals($criteria)) {
				$this->collArts = ArtPeer::doSelectJoinProv($criteria, $con, $join_behavior);
			}
		}
		$this->lastArtCriteria = $criteria;

		return $this->collArts;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tabulado is new, it will return
	 * an empty collection; or if this Tabulado has previously
	 * been saved, it will retrieve related Arts from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tabulado.
	 */
	public function getArtsJoinUnidadesRelatedByUniVenta($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collArts === null) {
			if ($this->isNew()) {
				$this->collArts = array();
			} else {

				$criteria->add(ArtPeer::TIPO_IMP, $this->tipo);

				$this->collArts = ArtPeer::doSelectJoinUnidadesRelatedByUniVenta($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ArtPeer::TIPO_IMP, $this->tipo);

			if (!isset($this->lastArtCriteria) || !$this->lastArtCriteria->equals($criteria)) {
				$this->collArts = ArtPeer::doSelectJoinUnidadesRelatedByUniVenta($criteria, $con, $join_behavior);
			}
		}
		$this->lastArtCriteria = $criteria;

		return $this->collArts;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tabulado is new, it will return
	 * an empty collection; or if this Tabulado has previously
	 * been saved, it will retrieve related Arts from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tabulado.
	 */
	public function getArtsJoinUnidadesRelatedBySuniVenta($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collArts === null) {
			if ($this->isNew()) {
				$this->collArts = array();
			} else {

				$criteria->add(ArtPeer::TIPO_IMP, $this->tipo);

				$this->collArts = ArtPeer::doSelectJoinUnidadesRelatedBySuniVenta($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ArtPeer::TIPO_IMP, $this->tipo);

			if (!isset($this->lastArtCriteria) || !$this->lastArtCriteria->equals($criteria)) {
				$this->collArts = ArtPeer::doSelectJoinUnidadesRelatedBySuniVenta($criteria, $con, $join_behavior);
			}
		}
		$this->lastArtCriteria = $criteria;

		return $this->collArts;
	}

	/**
	 * Clears out the collRengFacs collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addRengFacs()
	 */
	public function clearRengFacs()
	{
		$this->collRengFacs = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collRengFacs collection (array).
	 *
	 * By default this just sets the collRengFacs collection to an empty array (like clearcollRengFacs());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initRengFacs()
	{
		$this->collRengFacs = array();
	}

	/**
	 * Gets an array of RengFac objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Tabulado has previously been saved, it will retrieve
	 * related RengFacs from storage. If this Tabulado is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array RengFac[]
	 * @throws     PropelException
	 */
	public function getRengFacs($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collRengFacs === null) {
			if ($this->isNew()) {
			   $this->collRengFacs = array();
			} else {

				$criteria->add(RengFacPeer::TIPO_IMP, $this->tipo);

				RengFacPeer::addSelectColumns($criteria);
				$this->collRengFacs = RengFacPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(RengFacPeer::TIPO_IMP, $this->tipo);

				RengFacPeer::addSelectColumns($criteria);
				if (!isset($this->lastRengFacCriteria) || !$this->lastRengFacCriteria->equals($criteria)) {
					$this->collRengFacs = RengFacPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastRengFacCriteria = $criteria;
		return $this->collRengFacs;
	}

	/**
	 * Returns the number of related RengFac objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related RengFac objects.
	 * @throws     PropelException
	 */
	public function countRengFacs(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collRengFacs === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(RengFacPeer::TIPO_IMP, $this->tipo);

				$count = RengFacPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(RengFacPeer::TIPO_IMP, $this->tipo);

				if (!isset($this->lastRengFacCriteria) || !$this->lastRengFacCriteria->equals($criteria)) {
					$count = RengFacPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collRengFacs);
				}
			} else {
				$count = count($this->collRengFacs);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a RengFac object to this object
	 * through the RengFac foreign key attribute.
	 *
	 * @param      RengFac $l RengFac
	 * @return     void
	 * @throws     PropelException
	 */
	public function addRengFac(RengFac $l)
	{
		if ($this->collRengFacs === null) {
			$this->initRengFacs();
		}
		if (!in_array($l, $this->collRengFacs, true)) { // only add it if the **same** object is not already associated
			array_push($this->collRengFacs, $l);
			$l->setTabulado($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tabulado is new, it will return
	 * an empty collection; or if this Tabulado has previously
	 * been saved, it will retrieve related RengFacs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tabulado.
	 */
	public function getRengFacsJoinArt($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collRengFacs === null) {
			if ($this->isNew()) {
				$this->collRengFacs = array();
			} else {

				$criteria->add(RengFacPeer::TIPO_IMP, $this->tipo);

				$this->collRengFacs = RengFacPeer::doSelectJoinArt($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(RengFacPeer::TIPO_IMP, $this->tipo);

			if (!isset($this->lastRengFacCriteria) || !$this->lastRengFacCriteria->equals($criteria)) {
				$this->collRengFacs = RengFacPeer::doSelectJoinArt($criteria, $con, $join_behavior);
			}
		}
		$this->lastRengFacCriteria = $criteria;

		return $this->collRengFacs;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tabulado is new, it will return
	 * an empty collection; or if this Tabulado has previously
	 * been saved, it will retrieve related RengFacs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tabulado.
	 */
	public function getRengFacsJoinSubAlma($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collRengFacs === null) {
			if ($this->isNew()) {
				$this->collRengFacs = array();
			} else {

				$criteria->add(RengFacPeer::TIPO_IMP, $this->tipo);

				$this->collRengFacs = RengFacPeer::doSelectJoinSubAlma($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(RengFacPeer::TIPO_IMP, $this->tipo);

			if (!isset($this->lastRengFacCriteria) || !$this->lastRengFacCriteria->equals($criteria)) {
				$this->collRengFacs = RengFacPeer::doSelectJoinSubAlma($criteria, $con, $join_behavior);
			}
		}
		$this->lastRengFacCriteria = $criteria;

		return $this->collRengFacs;
	}

	/**
	 * Clears out the collDocumCcs collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addDocumCcs()
	 */
	public function clearDocumCcs()
	{
		$this->collDocumCcs = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collDocumCcs collection (array).
	 *
	 * By default this just sets the collDocumCcs collection to an empty array (like clearcollDocumCcs());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initDocumCcs()
	{
		$this->collDocumCcs = array();
	}

	/**
	 * Gets an array of DocumCc objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Tabulado has previously been saved, it will retrieve
	 * related DocumCcs from storage. If this Tabulado is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array DocumCc[]
	 * @throws     PropelException
	 */
	public function getDocumCcs($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDocumCcs === null) {
			if ($this->isNew()) {
			   $this->collDocumCcs = array();
			} else {

				$criteria->add(DocumCcPeer::TIPO, $this->tipo);

				DocumCcPeer::addSelectColumns($criteria);
				$this->collDocumCcs = DocumCcPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(DocumCcPeer::TIPO, $this->tipo);

				DocumCcPeer::addSelectColumns($criteria);
				if (!isset($this->lastDocumCcCriteria) || !$this->lastDocumCcCriteria->equals($criteria)) {
					$this->collDocumCcs = DocumCcPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastDocumCcCriteria = $criteria;
		return $this->collDocumCcs;
	}

	/**
	 * Returns the number of related DocumCc objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related DocumCc objects.
	 * @throws     PropelException
	 */
	public function countDocumCcs(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collDocumCcs === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(DocumCcPeer::TIPO, $this->tipo);

				$count = DocumCcPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(DocumCcPeer::TIPO, $this->tipo);

				if (!isset($this->lastDocumCcCriteria) || !$this->lastDocumCcCriteria->equals($criteria)) {
					$count = DocumCcPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collDocumCcs);
				}
			} else {
				$count = count($this->collDocumCcs);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a DocumCc object to this object
	 * through the DocumCc foreign key attribute.
	 *
	 * @param      DocumCc $l DocumCc
	 * @return     void
	 * @throws     PropelException
	 */
	public function addDocumCc(DocumCc $l)
	{
		if ($this->collDocumCcs === null) {
			$this->initDocumCcs();
		}
		if (!in_array($l, $this->collDocumCcs, true)) { // only add it if the **same** object is not already associated
			array_push($this->collDocumCcs, $l);
			$l->setTabulado($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tabulado is new, it will return
	 * an empty collection; or if this Tabulado has previously
	 * been saved, it will retrieve related DocumCcs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tabulado.
	 */
	public function getDocumCcsJoinClientes($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDocumCcs === null) {
			if ($this->isNew()) {
				$this->collDocumCcs = array();
			} else {

				$criteria->add(DocumCcPeer::TIPO, $this->tipo);

				$this->collDocumCcs = DocumCcPeer::doSelectJoinClientes($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DocumCcPeer::TIPO, $this->tipo);

			if (!isset($this->lastDocumCcCriteria) || !$this->lastDocumCcCriteria->equals($criteria)) {
				$this->collDocumCcs = DocumCcPeer::doSelectJoinClientes($criteria, $con, $join_behavior);
			}
		}
		$this->lastDocumCcCriteria = $criteria;

		return $this->collDocumCcs;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tabulado is new, it will return
	 * an empty collection; or if this Tabulado has previously
	 * been saved, it will retrieve related DocumCcs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tabulado.
	 */
	public function getDocumCcsJoinVendedor($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDocumCcs === null) {
			if ($this->isNew()) {
				$this->collDocumCcs = array();
			} else {

				$criteria->add(DocumCcPeer::TIPO, $this->tipo);

				$this->collDocumCcs = DocumCcPeer::doSelectJoinVendedor($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DocumCcPeer::TIPO, $this->tipo);

			if (!isset($this->lastDocumCcCriteria) || !$this->lastDocumCcCriteria->equals($criteria)) {
				$this->collDocumCcs = DocumCcPeer::doSelectJoinVendedor($criteria, $con, $join_behavior);
			}
		}
		$this->lastDocumCcCriteria = $criteria;

		return $this->collDocumCcs;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tabulado is new, it will return
	 * an empty collection; or if this Tabulado has previously
	 * been saved, it will retrieve related DocumCcs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tabulado.
	 */
	public function getDocumCcsJoinMonedaRelatedByMoneda($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDocumCcs === null) {
			if ($this->isNew()) {
				$this->collDocumCcs = array();
			} else {

				$criteria->add(DocumCcPeer::TIPO, $this->tipo);

				$this->collDocumCcs = DocumCcPeer::doSelectJoinMonedaRelatedByMoneda($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DocumCcPeer::TIPO, $this->tipo);

			if (!isset($this->lastDocumCcCriteria) || !$this->lastDocumCcCriteria->equals($criteria)) {
				$this->collDocumCcs = DocumCcPeer::doSelectJoinMonedaRelatedByMoneda($criteria, $con, $join_behavior);
			}
		}
		$this->lastDocumCcCriteria = $criteria;

		return $this->collDocumCcs;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tabulado is new, it will return
	 * an empty collection; or if this Tabulado has previously
	 * been saved, it will retrieve related DocumCcs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tabulado.
	 */
	public function getDocumCcsJoinAlmacen($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TabuladoPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDocumCcs === null) {
			if ($this->isNew()) {
				$this->collDocumCcs = array();
			} else {

				$criteria->add(DocumCcPeer::TIPO, $this->tipo);

				$this->collDocumCcs = DocumCcPeer::doSelectJoinAlmacen($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DocumCcPeer::TIPO, $this->tipo);

			if (!isset($this->lastDocumCcCriteria) || !$this->lastDocumCcCriteria->equals($criteria)) {
				$this->collDocumCcs = DocumCcPeer::doSelectJoinAlmacen($criteria, $con, $join_behavior);
			}
		}
		$this->lastDocumCcCriteria = $criteria;

		return $this->collDocumCcs;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collArts) {
				foreach ((array) $this->collArts as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collRengFacs) {
				foreach ((array) $this->collRengFacs as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collDocumCcs) {
				foreach ((array) $this->collDocumCcs as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collArts = null;
		$this->collRengFacs = null;
		$this->collDocumCcs = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseTabulado:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseTabulado::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseTabulado
